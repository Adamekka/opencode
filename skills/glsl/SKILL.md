---
name: glsl
description: When GLSL.
---

# GLSL Preferences

- Use the same GLSL `#version` for every shader in a codebase.
- Order shader declarations as follows, omitting sections that do not apply:
  1. Per-vertex inputs
  2. Uniform inputs
  3. Shader outputs
  4. Functions, including `main`
